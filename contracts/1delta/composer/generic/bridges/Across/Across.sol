// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

contract Across {
    function _bridgeAccross(uint256 currentOffset, address callerAddress) internal returns (uint256) {
        // TODO: Add across implementation
        return currentOffset;
    }
}
